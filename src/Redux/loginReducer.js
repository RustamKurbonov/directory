let initialState = {
    login: 'Admin',
    pass: '12345',
    return: false,
    errorOpacity: 0
}

const loginReducer = (state = initialState) => {

    return state
}

export default loginReducer;